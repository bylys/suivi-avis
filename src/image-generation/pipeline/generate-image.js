/**
 * pipeline/generate-image.js
 * Phase 6+ : contiendra _generateImageOnly.
 * Actuellement dans app.js lignes 13749–14008.
 * Orchestration : buildDallePromptV2 → _applySiteRealism → _applyVariation
 *   → _planGlobalBatch → _resolveLocationAndComposition → _validateResolvedScene
 *   → _validateLocationServiceCompatibility → _validateWorkerScene → _validateQuality
 *   → _rewritePromptWithGPT → _assertFinalWorkerConsistency → _appendLockedFinalConstraints
 *   → API Images → Vision safety check.
 */
// Stub Phase 0 — rien à exporter ici pour l'instant.
