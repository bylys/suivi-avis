/**
 * services/index.js
 * Phase 2+ : assemble SERVICE_VISUAL_RULES à partir de tous les fichiers
 * métier et vérifie l'absence de clés dupliquées.
 *
 * Exemple cible :
 *   export const SERVICE_VISUAL_RULES = {
 *     ...TOITURE_SERVICES,
 *     ...DEPANNAGE_AUTO_SERVICES,
 *     ...
 *   };
 *   assertNoDuplicateServiceKeys(SERVICE_VISUAL_RULES);
 */
// Stub Phase 0 — rien à exporter ici pour l'instant.
