/**
 * resolution/worker-resolver.js — Phase 3 shadow (source active : app.js)
 * _applyVariation déplacé dans scene-resolver.js (responsabilité scène complète).
 * Ce fichier re-exporte uniquement pour ne pas casser les imports de test existants.
 * Ne pas modifier avant le cutover validé.
 */

export { _applyVariation } from './scene-resolver.js';
