/**
 * prompt/scene-builder.js
 * Phase 5+ : contiendra buildDallePromptV2, _getWorkDetail, _getCityContext,
 * INTERIOR_SCENE_BASE, PHOTO_STYLE_RULES.
 * Actuellement dans app.js lignes 4518–4674 + 13133–13241.
 *
 * Dépendances : WORK_SCENES, SITE_REALISM, _lastMatch (état partagé),
 *   _hashSeed, SERVICE_CATALOG, VARIATION_ENGINE.
 * Point d'attention : _lastMatch est le seul pont entre le domaine GMB
 *   (detecterMetier) et le domaine image-gen. À gérer dans pipeline/state.js.
 */
// Stub Phase 0 — rien à exporter ici pour l'instant.
